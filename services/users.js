// TODO: Abstract DB functions.
// class Users {
        
//     constructor(connection) {
//         this.connection = connection;
//     }

//     getUser() {
//         // Look up the newly created record.
//         this.connection.query('SELECT * FROM users WHERE id = ?', req.body['user_id'], function(error, results, fields) {

//         });
//     }
// }

// module.exports = {users}